title: |
  {{- if eq .status "firing" -}}
  🚨 {{ range .alerts }}{{ .labels.alertname }}{{ end }}
  {{- else if eq .status "resolved" -}}
  ✅ {{ range .alerts }}{{ .labels.alertname }}{{ end }}
  {{- else -}}
  ⚠️ Alert
  {{- end }}
message: |
  {{- range .alerts -}}
  {{- if eq $.status "firing" -}}
  {{ .labels.job }} service is down on {{ .labels.host }}
  {{- else if eq $.status "resolved" -}}
  {{ .labels.job }} service is up on {{ .labels.host }}
  {{- else if .annotations.summary }}
  {{ .annotations.summary }}
  {{- else }}
  Alert: {{ .labels.alertname }}
  {{- end }}
  {{- end }}
priority: |
  {{- if eq .status "firing" -}}
  4
  {{- else -}}
  3
  {{- end }}
tags: |
  {{- if eq .status "firing" -}}
  grafana,warning
  {{- else -}}
  grafana,white_check_mark
  {{- end }}
